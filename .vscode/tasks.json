{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Qt Build",
            "type": "shell",
            "command": "cmake --build ${command:cmake.buildDirectory} --config ${command:cmake.buildType}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}